#include <stdio.h>

/*
 * Main - this is the entry point
 *@a - the integer value to be processed
 * Return: Always 0
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a)i;
	putchar ('\n');
	return (0);
}
