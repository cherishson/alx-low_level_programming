#include <stdio.h>

/**
 * main -a functionthat prints the alphabets in lowercase
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char x;

	x = 'a';

	while
		(x <= 'z')
		{putchar (x);
		x++;
		}

		putchar('\n');
	return (0);
}
